### This project is based on supervised machine learning for predicting the employee turnover. Based on the various factors model predicts whether the employee will leave the company (1) or the employee stays in the company(0).

#### The model used is Logistic Regression as baseline model.
#### The L1(Lasso) and L2(Ridge) regularisation techniques are applied on the model to check which gives better results.
